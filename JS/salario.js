const salario = 1430500;
let valor_extra = (salario/220)*1.25 ;
cantidad=prompt("ingrese el numero de horas extras");
//let total =Math.round(valor_extra*cantidad );
alert("El valor a pagar es " + Math.round(valor_extra*cantidad ));
